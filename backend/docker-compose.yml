services:
  app:
    build: .
    container_name: task-manager-backend
    # Ingests your local .env file into the container
    env_file:
      - .env
    ports:
      - "5001:5001"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules